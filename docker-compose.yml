services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./ru_groups_session.session:/app/ru_groups_session.session
    restart: unless-stopped
